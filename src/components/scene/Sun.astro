<svg class="sun" viewBox="0 0 1200 800" preserveAspectRatio="xMidYMid slice" aria-hidden="true" focusable="false">
  <defs>
    <radialGradient id="sunHalo" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="var(--halo)" stop-opacity="0.85"></stop>
      <stop offset="60%" stop-color="var(--halo)" stop-opacity="0.22"></stop>
      <stop offset="100%" stop-color="var(--halo)" stop-opacity="0"></stop>
    </radialGradient>
  </defs>
  <circle cx="726" cy="132" r="300" fill="url(#sunHalo)"></circle>
  <circle cx="726" cy="132" r="48" fill="var(--light)"></circle>
</svg>

<style>
  .sun {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100vh;
    opacity: var(--sun-opacity);
    transition: opacity 600ms ease;
  }
</style>
